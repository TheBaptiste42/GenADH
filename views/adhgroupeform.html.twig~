{% extends "layout.html.twig" %}

{% block title %}Gestion des groupes{% endblock %}

{% block content %}
{% if groupeForm %}
	{% if groupe.id %}
	<h1>Modification de {{ groupe.libelle }}</h1><br>
	{% else %}
	<h1>Ajout d'un groupe</h1><br>
	{% endif %}
	{{ form_start(groupeForm) }}
		<div class="col-lg-8 form-group">
			{{ form_widget(groupeForm.libelle, { 'attr':  {
				'class': 'form-control',
				'placeholder': 'Libelle du groupe' 
			}}) }}	
		</div><div class="col-lg-4 form-group">
			{{ form_widget(groupeForm.typecotisation, { 'attr':  {
				'class': 'form-control'
			}}) }}
		</div>
		<div class="col-lg-12 form-group">
			<input type="submit" class="col-lg-12 btn btn-success" id="bt_hh" value="Valider" />
		</div>
	{{ form_end(groupeForm) }}
	{% if groupe.id %}
		<div class="col-lg-12 form-group"><a class="btn btn-primary col-lg-12" href="{{ path('groupe',{'id':groupe.id}) }}">Retour à la fiche utilisateur</a></div>
	{% else %}
		<div class="col-lg-12 form-group"><a class="btn btn-danger col-lg-12" href="{{ path('groupe-list') }}">Annuler l'ajout et retour à la liste</a></div>
	{% endif %}
	{% for flashMessage in app.session.flashbag.get('success') %}
		<div class="col-lg-12 alert alert-success">
			{{ flashMessage }}
		</div>
	{% endfor %}
	
{% else %}
	<br><br>
	<div class="alert alert-danger" role="alert">Vous devez être connecté pour modifier un adhérent !</div>
{% endif %}
{% endblock %}
{% block scripts %}
<script>
</script>
{% endblock %}