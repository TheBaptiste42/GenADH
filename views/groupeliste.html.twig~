{% extends "layout.html.twig" %}

{% block title %}Liste des groupes{% endblock %}

{% block content %}
<h3>Liste des groupes</h3>
<div class="col-lg-12 form-group"><a class="btn btn-success col-lg-12" href="{{ path('groupe-add') }}">Ajouter un groupe</a></div>
<div class="col-lg-12">
<table class="table table-bordered table-striped table-condensed" id="table_adh">
	<thead>
		<tr>
			<th>Id</th>
			<th>Libellé</th>
			<th>Statut</th>
		</tr>
	</thead>
	<tbody>
	{% for groupe in groupes %}
		<tr>
			<td>{{ groupe.id }}</td>
			<td><a href="{{ path('groupe',{'id':groupe.id}) }}">{{ groupe.libelle }}</a></td>
			<td>
			{% if groupe.typecotisation %}
				<span class="label label-success">Ce groupe doit payer des cotisations</span>
			{% else %}
				<span class="label label-info">Ce groupe est exempt de cotisations</span>
			{% endif %}
			</td>
		</tr>
	{% endfor %}
	</tbody>
</table>
</div>
{% for flashMessage in app.session.flashbag.get('danger') %}
	<div class="col-lg-12 alert alert-danger">
		{{ flashMessage }}
	</div>
{% endfor %}
{% for flashMessage in app.session.flashbag.get('info') %}
	<div class="col-lg-12 alert alert-info">
		{{ flashMessage }}
	</div>
{% endfor %}
{% endblock %}
{% block scripts %}
<script>
</script>
{% endblock %}
